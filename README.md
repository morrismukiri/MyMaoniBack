# My Maoni Backend and API
provide an admin interface for the app

## Install

From the root Run

`$ composer install`

Then

`$ npm install `

`$ bower install`

Then run migrations

`$ php artisan migrate`


